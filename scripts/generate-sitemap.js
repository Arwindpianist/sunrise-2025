/**
 * Next.js serves the sitemap from `app/sitemap.ts` at runtime.
 * This script is a no-op placeholder so `pnpm sitemap` does not fail if CI calls it.
 */
console.log(
  "Sitemap is defined in apps/sunrise-web/app/sitemap.ts (dynamic route). No static file to generate.",
)
process.exit(0)
